import React from 'react';

const FooterComponent = () => {
    return(
        <div>
        <footer className="footer">
        <span className="text-muted">Copy rights: @Grocers</span>
        </footer>
        </div>
    );
};

export default FooterComponent;